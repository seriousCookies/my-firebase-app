steps:
  # Install
  - name: "gcr.io/cloud-builders/npm"
    args: ["install"]
  # Build
  - name: "gcr.io/cloud-builders/npm"
    args: ["run", "build"]
  # Deploy
  - name: gcr.io/my-first-firebase-8edd4/firebase
    args: ["deploy", "--project=my-first-firebase-8edd4", "--only=hosting"]
